'use client';

import AnimatedBackground from '../AnimatedBackground';

const ServicesBackground: React.FC = () => {
  return <AnimatedBackground variant="services" />;
};

export default ServicesBackground;
