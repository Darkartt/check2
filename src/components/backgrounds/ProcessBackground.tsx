'use client';

import AnimatedBackground from '../AnimatedBackground';

const ProcessBackground: React.FC = () => {
  return <AnimatedBackground variant="process" />;
};

export default ProcessBackground;
