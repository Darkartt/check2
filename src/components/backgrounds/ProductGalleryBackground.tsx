'use client';

import AnimatedBackground from '../AnimatedBackground';

const ProductGalleryBackground: React.FC = () => {
  return <AnimatedBackground variant="shop" />;
};

export default ProductGalleryBackground;
